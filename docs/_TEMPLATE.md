# <System / Format name>

**Source files:** `src/foo.cpp`, `src/foo.h` (cite the authoritative functions/structs)
**Status:** 🟡 partial / ✅ done

## Purpose
One paragraph: what this is and where it sits in the game.

## Data structures
The key structs/enums with field meanings. Quote `file:line`.

## Format / Algorithm
The exact on-disk grammar (for formats) or step-by-step behavior and formulas (for systems).
Be precise about field order, units, and ranges — a rewrite must reproduce these.

## RotS-specific notes
How this differs from stock CircleMUD/DikuMUD.

## Worked example
A real (or hand-built, clearly labeled) example, annotated field-by-field.

## Open questions
Anything the source left ambiguous. Better to list than to guess.
